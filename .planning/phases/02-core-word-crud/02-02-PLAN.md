---
phase: 02-core-word-crud
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/features/words/api/wordsApi.ts
  - frontend/src/features/words/hooks/useWords.ts
  - frontend/src/features/words/hooks/useCreateWord.ts
  - frontend/src/features/words/components/SelectField.tsx
  - frontend/src/features/words/components/WordClassBadge.tsx
  - frontend/src/features/words/components/GenderBadge.tsx
  - frontend/src/features/words/components/WordListRow.tsx
  - frontend/src/features/words/components/WordList.tsx
  - frontend/src/features/words/components/FAB.tsx
  - frontend/src/features/words/components/AddWordDrawer.tsx
  - frontend/src/pages/WordsPage.tsx
autonomous: true
requirements:
  - WORD-02

must_haves:
  truths:
    - "Сторінка /words показує список слів або skeleton під час завантаження"
    - "Порожній стан показує текст 'Словник порожній' з підказкою натиснути +"
    - "FAB '+' кнопка розташована fixed bottom-6 right-6 і відкриває AddWordDrawer"
    - "AddWordDrawer показує форму з полями: headword (required), translation, wordClass select, gender select, notes"
    - "Успішне збереження закриває drawer, додає слово до списку, показує toast 'Слово збережено'"
    - "Дублікат headword показує toast 'Слово «{headword}» вже є у вашому словнику'"
    - "Кожен рядок списку показує headword + translation + WordClassBadge + GenderBadge (якщо gender != null)"
  artifacts:
    - path: "frontend/src/features/words/api/wordsApi.ts"
      provides: "Word type + API functions"
      exports: ["Word", "CreateWordInput", "listWords", "createWord"]
    - path: "frontend/src/features/words/hooks/useWords.ts"
      provides: "useQuery(['words'])"
      exports: ["useWords"]
    - path: "frontend/src/features/words/hooks/useCreateWord.ts"
      provides: "useMutation for POST /words"
      exports: ["useCreateWord"]
    - path: "frontend/src/features/words/components/AddWordDrawer.tsx"
      provides: "DaisyUI modal з формою додавання"
      contains: "dialog modal-bottom sm:modal-middle"
    - path: "frontend/src/features/words/components/WordList.tsx"
      provides: "список слів з empty/loading/error станами"
    - path: "frontend/src/pages/WordsPage.tsx"
      provides: "WordList + FAB + AddWordDrawer з'єднані"
  key_links:
    - from: "frontend/src/pages/WordsPage.tsx"
      to: "frontend/src/features/words/hooks/useWords.ts"
      via: "useWords()"
      pattern: "useWords"
    - from: "frontend/src/features/words/components/AddWordDrawer.tsx"
      to: "frontend/src/features/words/hooks/useCreateWord.ts"
      via: "useCreateWord()"
      pattern: "useCreateWord"
    - from: "frontend/src/features/words/api/wordsApi.ts"
      to: "frontend/src/lib/api.ts"
      via: "import api"
      pattern: "api\\.(get|post)"
---

## Phase Goal

**As a** Norwegian learner, **I want to** add words to my dictionary and see them in a list, **so that** I can start building my vocabulary immediately.

<objective>
Реалізувати вертикальний зріз "додати слово і побачити список" — API layer (wordsApi.ts), TanStack Query hooks, UI компоненти (badges, row, list, FAB, AddWordDrawer) і зібрати все в WordsPage.

Purpose: Після цього плану користувач може перейти на /words, натиснути FAB, заповнити форму і побачити збережене слово в списку. Перший реальний сценарій використання застосунку.

Output: 11 нових/змінених файлів в frontend/src/features/words/ та frontend/src/pages/WordsPage.tsx.
</objective>

<execution_context>
@C:\Users\Олександр\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Олександр\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@c:\Users\Олександр\Desktop\lern nor\.planning\PROJECT.md
@c:\Users\Олександр\Desktop\lern nor\.planning\STATE.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\02-core-word-crud\02-CONTEXT.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\02-core-word-crud\02-UI-SPEC.md

<interfaces>
<!-- Контракти які виконавець повинен знати. Читати ПЕРЕД написанням коду. -->

From frontend/src/lib/api.ts:
  import api from '../../../lib/api';  // Axios instance з JWT interceptor
  Базовий URL: VITE_API_URL ?? 'http://localhost:3000/api'
  401 response автоматично викликає useAuthStore.getState().logout()

From frontend/src/lib/toastStore.ts:
  import { toast } from '../../../lib/toastStore';
  toast.success('msg') — зелений toast 10с
  toast.error('msg') — червоний toast 10с

From frontend/src/components/Button.tsx:
  interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>
  props: variant? ('primary'|'ghost'|'error'), loading? (boolean), block? (boolean)
  Використання: <Button variant="ghost" onClick={fn}>Скасувати</Button>
  Auto-disabled коли loading=true.

From frontend/src/components/Input.tsx:
  interface InputProps extends React.InputHTMLAttributes<HTMLInputElement>
  required props: id (string), label (string)
  optional: error? (string)
  Pattern: form-control > label[htmlFor] > input[aria-invalid aria-describedby] > error span[role=alert]
  Textarea: Input НЕ підтримує textarea — для notes використай окремий <textarea className="textarea textarea-bordered w-full">

From frontend/src/components/AppShell.tsx:
  Обгортає весь контент сторінки. max-w-7xl mx-auto px-4 py-6.
  Navbar вже всередині. Просто <AppShell>{children}</AppShell>.

From backend/prisma/schema.prisma (Word model для TypeScript типу):
  Gender enum: masculine | feminine | neuter  (3 значення — ОБОВ'ЯЗКОВО feminine!)
  WordClass enum: noun | verb | adjective | adverb | other
  Difficulty enum: A1 | A2 | B1 | B2 | C1 | C2

From TanStack Query v5 (ВАЖЛИВО — не v4):
  isPending — НЕ isLoading (isLoading видалено в v5)
  queryKey обов'язковий
  onError на useQuery видалено в v5 — використовуй isError + error
  QueryClientProvider вже налаштований в main.tsx (staleTime: 5min, retry: 1)

DaisyUI v4 modal pattern:
  <dialog ref={dialogRef} className="modal modal-bottom sm:modal-middle" onClose={onClose}>
    <div className="modal-box max-w-lg w-full mx-auto">...</div>
    <form method="dialog" className="modal-backdrop"><button onClick={onClose}>close</button></form>
  </dialog>
  dialogRef.current.showModal() / dialogRef.current.close() в useEffect([open])
  НЕ modal-open CSS клас (DaisyUI v3 — застаріло)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API layer + TanStack Query hooks + badge/row/list компоненти</name>
  <files>
    frontend/src/features/words/api/wordsApi.ts,
    frontend/src/features/words/hooks/useWords.ts,
    frontend/src/features/words/hooks/useCreateWord.ts,
    frontend/src/features/words/components/SelectField.tsx,
    frontend/src/features/words/components/WordClassBadge.tsx,
    frontend/src/features/words/components/GenderBadge.tsx,
    frontend/src/features/words/components/WordListRow.tsx,
    frontend/src/features/words/components/WordList.tsx
  </files>
  <read_first>
    - frontend/src/lib/api.ts — Axios instance import pattern
    - frontend/src/lib/toastStore.ts — toast.success / toast.error signatures
    - frontend/src/components/Input.tsx — повний файл для клонування патерну в SelectField
    - frontend/src/features/words/api/wordsApi.ts — не існує, треба створити
    - c:\Users\Олександр\Desktop\lern nor\.planning\phases\02-core-word-crud\02-UI-SPEC.md — badge кольори, copywriting, layout
  </read_first>
  <behavior>
    wordsApi.ts:
    - Word interface включає всі поля з Prisma моделі (id, userId, headword, translation, gender, wordClass, difficulty, notes, personalNote, forms, examples, createdAt, updatedAt)
    - gender тип: 'masculine' | 'feminine' | 'neuter' | null  (всі 3 значення)
    - listWords() → api.get('/words') → Promise<Word[]>
    - createWord(input) → api.post('/words', input) → Promise<Word>

    useWords():
    - useQuery({ queryKey: ['words'], queryFn: listWords })
    - повертає { data, isPending, isError }

    useCreateWord():
    - useMutation({ mutationFn: createWord })
    - onSuccess: invalidateQueries(['words']) + toast.success('Слово збережено')
    - onError (status 409): toast.error(`Слово «${headword}» вже є у вашому словнику`) — headword береться з err.config?.data
    - onError (інше): toast.error('Не вдалося зберегти слово. Спробуйте ще раз.')

    SelectField:
    - Клонує Input.tsx патерн: form-control > label[htmlFor] > select[aria-invalid aria-describedby] > error span
    - Props: id, label, error?, children (option elements), + всі SelectHTMLAttributes
    - select className: "select select-bordered w-full" + "select-error" якщо error

    WordClassBadge:
    - WORD_CLASS_LABELS: { noun: 'іменник', verb: 'дієслово', adjective: 'прикметник', adverb: 'прислівник', other: 'інше' }
    - DaisyUI class: "badge badge-neutral badge-sm"
    - prop: wordClass: string

    GenderBadge:
    - GENDER_LABELS: { masculine: 'ч', feminine: 'ж', neuter: 'с' }  (ОБОВ'ЯЗКОВО всі 3!)
    - DaisyUI class: "badge badge-ghost badge-sm"
    - prop: gender: string
    - Renders only when gender != null (умова на рівні батьківського компонента)

    WordListRow (per D-01, D-02, D-03, D-04):
    - flex items-center gap-3 px-4 py-3 border-b border-base-300 cursor-pointer hover:bg-base-200 active:bg-base-300
    - headword: text-base font-semibold flex-1
    - translation: text-sm text-base-content/70 flex-1 truncate (тільки якщо translation != null)
    - badges div: flex gap-1 shrink-0 — спочатку GenderBadge (тільки якщо gender != null), потім WordClassBadge
    - onClick prop → onWordClick(word.id)
    - aria: role="button", tabIndex=0, onKeyDown Enter/Space → onClick

    WordList:
    - isPending → 5 skeleton рядків: <div className="animate-pulse bg-base-300 rounded h-12 mx-4 my-1" />
    - isError → <p className="text-error text-center py-8">Не вдалося завантажити слова. Оновіть сторінку.</p>
    - words.length === 0 → empty state: flex flex-col items-center justify-center min-h-[60vh], <h2 className="text-xl font-semibold mb-2">Словник порожній</h2>, <p className="text-base-content/50">Натисніть + щоб додати перше слово.</p>
    - words.length > 0 → <ul className="divide-y divide-base-300">{words.map(w => <WordListRow .../>)}</ul>
    - Props: words: Word[], isPending: boolean, isError: boolean, onWordClick: (id: string) => void
  </behavior>
  <action>
    Створи директорію frontend/src/features/words/ з підпапками api/, hooks/, components/ (якщо не існують).

    1. wordsApi.ts — TypeScript interfaces Word, CreateWordInput (headword: string, translation?: string, gender?: Word['gender'], wordClass?: Word['wordClass'], notes?: string). Функції listWords і createWord через `import api from '../../../lib/api'`. Функцію getWord теж додай (для Plan 03): getWord(id: string): Promise<Word> → api.get('/words/${id}').

    2. useWords.ts — мінімальна обгортка useQuery. Файл ~10 рядків.

    3. useCreateWord.ts — useMutation з onSuccess (invalidateQueries + toast.success) і onError (409 detection + toast.error). Headword для 409 toast витягай з: `(err as any).config?.data ? JSON.parse((err as any).config.data).headword : ''`.

    4. SelectField.tsx — клонуй структуру Input.tsx замінивши <input className="input input-bordered"> на <select className="select select-bordered"> + children prop. id, label, error props ідентичні.

    5. WordClassBadge.tsx і GenderBadge.tsx — прості компоненти з lookup map → badge span.

    6. WordListRow.tsx — li елемент з onClick + keyboard handler. Умовний рендер translation і GenderBadge.

    7. WordList.tsx — компонент з трьома станами (pending/error/data) відповідно до behavior вище.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\frontend" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/words/api/wordsApi.ts: Word interface містить gender типу 'masculine' | 'feminine' | 'neuter' | null (всі 3)
    - frontend/src/features/words/hooks/useWords.ts: використовує isPending (grep -n "isPending" підтверджує; grep для "isLoading" не знаходить нічого)
    - frontend/src/features/words/components/GenderBadge.tsx: GENDER_LABELS містить ключ 'feminine' (grep -n "feminine" підтверджує)
    - frontend/src/features/words/components/SelectField.tsx: містить "select select-bordered" і "form-control" (grep підтверджує)
    - frontend/src/features/words/components/WordListRow.tsx: містить role="button" або tabIndex
    - frontend/src/features/words/components/WordList.tsx: містить "Словник порожній" і "animate-pulse"
    - TypeScript компілюється без помилок (npx tsc --noEmit)
  </acceptance_criteria>
  <done>8 файлів створені, TypeScript чистий, gender тип включає feminine.</done>
</task>

<task type="auto">
  <name>Task 2: FAB + AddWordDrawer + WordsPage assembly</name>
  <files>
    frontend/src/features/words/components/FAB.tsx,
    frontend/src/features/words/components/AddWordDrawer.tsx,
    frontend/src/pages/WordsPage.tsx
  </files>
  <read_first>
    - frontend/src/components/AppShell.tsx — props interface і використання
    - frontend/src/components/Button.tsx — повний файл
    - frontend/src/components/Input.tsx — pattern для форми
    - frontend/src/features/words/api/wordsApi.ts — CreateWordInput interface (щойно створений)
    - frontend/src/features/words/hooks/useCreateWord.ts — mutate signature
    - c:\Users\Олександр\Desktop\lern nor\.planning\phases\02-core-word-crud\02-UI-SPEC.md — AddWordDrawer layout і copywriting
  </read_first>
  <action>
    Створи FAB.tsx:
    - fixed bottom-6 right-6 z-50, btn btn-primary btn-circle w-14 h-14 shadow-lg
    - aria-label="Додати слово"
    - Контент кнопки: текст "+" або SVG plus icon
    - Props: { onClick: () => void }

    Створи AddWordDrawer.tsx:
    - Props: { open: boolean; onClose: () => void }
    - useRef<HTMLDialogElement>(null) + useEffect([open]): if(open) dialogRef.current?.showModal(); else dialogRef.current?.close();
    - onClose handler прив'язаний до dialog onClose event (ESC та backdrop)
    - useCreateWord() hook
    - Локальний стан форми: headword (string), translation (string), gender ('masculine'|'feminine'|'neuter'|''), wordClass ('noun'|'verb'|'adjective'|'adverb'|'other'|''), notes (string)
    - headwordError стан для валідації
    - onClose також скидає форму до порожніх значень
    - handleSubmit: якщо headword.trim() порожній — встановити headwordError 'Слово обов'язкове'; інакше mutation.mutate({ headword: headword.trim(), translation: translation || undefined, gender: gender || undefined, wordClass: wordClass || undefined, notes: notes || undefined })
    - mutation.isSuccess в useEffect: викликати onClose
    - Modal structure (per D-05, UI-SPEC):
      <dialog ref={dialogRef} className="modal modal-bottom sm:modal-middle" onClose={onClose}>
        <div className="modal-box max-w-lg w-full mx-auto">
          <h3 className="text-xl font-semibold mb-4">Нове слово</h3>
          <form onSubmit={handleSubmit} className="flex flex-col gap-4">
            <Input id="headword" label="Слово (нюнорськ)" value={headword} onChange={...} error={headwordError} required />
            <Input id="translation" label="Переклад" value={translation} onChange={...} />
            <SelectField id="wordClass" label="Клас слова" value={wordClass} onChange={...}>
              <option value="">Оберіть клас слова</option>
              <option value="noun">іменник</option>
              <option value="verb">дієслово</option>
              <option value="adjective">прикметник</option>
              <option value="adverb">прислівник</option>
              <option value="other">інше</option>
            </SelectField>
            <SelectField id="gender" label="Рід" value={gender} onChange={...}>
              <option value="">— (без роду)</option>
              <option value="masculine">чоловічий</option>
              <option value="feminine">жіночий</option>
              <option value="neuter">середній</option>
            </SelectField>
            <div className="form-control w-full">
              <label className="label" htmlFor="notes"><span className="label-text font-semibold">Нотатки</span></label>
              <textarea id="notes" className="textarea textarea-bordered w-full" value={notes} onChange={...} rows={3} />
            </div>
            <div className="modal-action gap-2">
              <Button type="button" variant="ghost" onClick={onClose}>Скасувати</Button>
              <Button type="submit" loading={mutation.isPending}>{mutation.isPending ? 'Зберігаю…' : 'Зберегти'}</Button>
            </div>
          </form>
        </div>
        <form method="dialog" className="modal-backdrop"><button onClick={onClose}>close</button></form>
      </dialog>

    Заміни frontend/src/pages/WordsPage.tsx повністю (поточний — placeholder):
    - import useWords, WordList, FAB, AddWordDrawer
    - import AppShell
    - Локальний стан: addDrawerOpen (boolean, false), selectedWordId (string | null, null)
    - const { data: words, isPending, isError } = useWords()
    - return:
      <AppShell>
        <WordList words={words ?? []} isPending={isPending} isError={isError} onWordClick={setSelectedWordId} />
        <FAB onClick={() => setAddDrawerOpen(true)} />
        <AddWordDrawer open={addDrawerOpen} onClose={() => setAddDrawerOpen(false)} />
      </AppShell>
    - selectedWordId залишається для Plan 03 (WordDetailDrawer)
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\frontend" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/words/components/FAB.tsx: містить "btn-circle" і aria-label="Додати слово"
    - frontend/src/features/words/components/AddWordDrawer.tsx:
      - містить "modal modal-bottom sm:modal-middle"
      - містить useRef та useEffect з showModal / close
      - gender select містить option value="feminine" (grep -n "feminine" підтверджує)
      - НЕ містить "modal-open" CSS клас
    - frontend/src/pages/WordsPage.tsx: більше НЕ містить "Функція додавання слів" (старий placeholder текст)
    - frontend/src/pages/WordsPage.tsx: містить WordList, FAB, AddWordDrawer
    - TypeScript компілюється без помилок
    - Запустити dev server: http://localhost:5173/words відображає порожній стан або список без JS помилок в консолі
  </acceptance_criteria>
  <done>Користувач може перейти на /words, натиснути FAB, заповнити форму з headword і побачити слово в списку після збереження. Toast "Слово збережено" з'являється.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddWordDrawer form → API | User input: headword, translation, gender, wordClass, notes |
| useCreateWord onError | HTTP error response handled client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | AddWordDrawer form | mitigate | headword required validation (non-empty trim check) before mutation.mutate |
| T-02-08 | Information Disclosure | useWords list | mitigate | API instance sends JWT; backend filters by userId; client receives only own words |
| T-02-09 | Denial of Service | FAB rapid clicks | accept | mutation.isPending disables submit button; duplicate requests prevented by backend 409 |
</threat_model>

<verification>
Ручна перевірка після виконання:
1. Запусти backend (cd backend && npm run dev) і frontend (cd frontend && npm run dev)
2. Відкрий http://localhost:5173, увійди в систему
3. Перейди на /words — побачи порожній стан з текстом "Словник порожній"
4. Натисни FAB "+" — відкривається drawer "Нове слово"
5. Введи headword "hus", translation "будинок", wordClass "noun", gender "neuter"
6. Натисни "Зберегти" — кнопка показує spinner → drawer закривається → слово з'являється в списку → toast "Слово збережено"
7. Знову натисни FAB, введи "hus" → "Зберегти" → toast про дублікат
8. Рядок списку показує: "hus" + "будинок" + badge "іменник" + badge "с"
</verification>

<success_criteria>
- Сторінка /words відображає список слів або empty state
- FAB відкриває AddWordDrawer
- Збереження слова: drawer закривається, список оновлюється, toast 'Слово збережено'
- Дублікат headword: toast 'Слово «hus» вже є у вашому словнику'
- TypeScript чистий на всіх нових файлах
- Gender select містить masculine, feminine, neuter (всі 3)
</success_criteria>

<output>
Після завершення створи `.planning/phases/02-core-word-crud/02-02-SUMMARY.md`
</output>
