---
phase: 06-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/WordsPage.tsx
  - frontend/src/features/words/components/WordDetailDrawer.tsx
  - frontend/src/features/words/hooks/useAnalysisChat.ts
  - frontend/src/features/words/hooks/useWordChat.ts
  - backend/src/services/words.ts
  - backend/src/index.ts
  - backend/src/routes/auth.ts
  - frontend/src/features/translate/components/TranslatorPanel.tsx
  - frontend/src/features/translate/hooks/useTranslate.ts
  - frontend/src/features/translate/api/translateApi.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05]

must_haves:
  truths:
    - "FAB кнопка '+' з'являється на вкладці Словник і відкриває AddWordDrawer"
    - "WordDetailDrawer в режимі edit показує і зберігає forms, examples, difficulty"
    - "Видалення слова вимагає натискання другої кнопки підтвердження"
    - "Помилка надсилання у чаті показує toast замість тихого зникання повідомлення"
    - "Translator показує лічильник символів; fallback warning — покращений текст"
    - "Backend PATCH не приймає поля userId/createdAt у тілі запиту"
    - "Глобальний Express error handler повертає { error: 'Internal server error' } без stack trace"
    - "Глобальний React ErrorBoundary показує Ukrainian fallback замість білого екрану"
  artifacts:
    - path: "frontend/src/pages/WordsPage.tsx"
      provides: "FAB + AddWordDrawer wired on dictionary tab"
    - path: "frontend/src/features/words/components/WordDetailDrawer.tsx"
      provides: "Full edit form (10 fields) + delete confirmation"
    - path: "frontend/src/features/words/hooks/useAnalysisChat.ts"
      provides: "toast.error on chat send failure"
    - path: "frontend/src/features/words/hooks/useWordChat.ts"
      provides: "toast.error on chat send failure"
    - path: "backend/src/services/words.ts"
      provides: "Field whitelist in updateWord"
    - path: "backend/src/index.ts"
      provides: "Global Express error handler middleware"
    - path: "backend/src/routes/auth.ts"
      provides: "try/catch on register and login handlers"
    - path: "frontend/src/features/translate/components/TranslatorPanel.tsx"
      provides: "Char counter, fixed lastKeyRef, abort on unmount, showLoading fix, better fallback text"
    - path: "frontend/src/features/translate/api/translateApi.ts"
      provides: "Dead requestSignal() function removed"
    - path: "frontend/src/App.tsx"
      provides: "React ErrorBoundary wrapping Routes"
  key_links:
    - from: "frontend/src/pages/WordsPage.tsx"
      to: "FAB + AddWordDrawer components"
      via: "import + addOpen state + dictionary tab render"
      pattern: "addOpen"
    - from: "backend/src/services/words.ts updateWord"
      to: "prisma.word.update"
      via: "destructured whitelist object — only named fields passed as data"
      pattern: "const.*headword.*translation.*gender"
---

<objective>
Full-project audit polish: wire the dead manual add-word flow (FAB + AddWordDrawer), expand
the edit form to cover all 10 word fields, add UX safety guards (delete confirm, chat error
toasts), harden the backend against field injection and stack-trace leaks, and polish the
Translator tab (char counter, abort, deduplication fix, dead code cleanup). Adds a global
React ErrorBoundary so any render crash shows a readable Ukrainian message instead of a blank
screen.

Purpose: Close the highest-severity audit findings from RESEARCH.md — B-01, P-02, U-07, P-06,
U-05, B-02, B-09, B-10, S-01, S-02, S-05, U-04, U-06, U-10 — in a single focused pass.

Output:
- Manual word entry flow reachable from the dictionary tab
- Complete edit mode in WordDetailDrawer (all editable fields)
- Delete confirmation step on mobile-friendly touch targets
- Chat error toasts in both chat hooks
- Backend: field whitelist + global error handler + auth try/catch
- Translator: char counter + abort + fixed dedup ref + improved fallback text + dead code removed
- React ErrorBoundary in App.tsx
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-CONTEXT.md
@.planning/phases/06-polish/06-RESEARCH.md
</context>

<interfaces>
From frontend/src/features/words/components/FAB.tsx:
  export function FAB({ onClick }: { onClick: () => void })
  — Fixed positioned, bottom-6 right-6 z-50, btn-primary btn-circle, aria-label "Додати слово"
  NOTE: z-50 may conflict with the chat FAB (z-40). Place the add-word FAB at bottom-6 left-6
  (or another non-overlapping corner) OR change the dictionary-tab FAB to bottom-24 right-6 to
  clear the tab bar. Pick whichever avoids overlap with the existing chat FAB at bottom-20 right-6.

From frontend/src/features/words/components/AddWordDrawer.tsx:
  export function AddWordDrawer({ open, onClose }: { open: boolean; onClose: () => void })
  — Full AI-fill drawer with form fields; already handles createWord mutation internally.

From frontend/src/features/words/api/wordsApi.ts:
  export interface Word {
    id, userId, headword, translation, gender, wordClass, difficulty,
    notes, personalNote, forms: WordForms | null, examples: string[],
    tags: WordTag[], rawAiOutput, createdAt, updatedAt
  }
  export interface UpdateWordInput extends Partial<CreateWordInput> { personalNote?: string }
  export type Difficulty = 'A1' | 'A2' | 'B1' | 'B2' | 'C1' | 'C2'
  export type WordForms = Record<string, string>

From frontend/src/lib/toastStore.ts (imported in both chat hooks as `import { toast } from '../../../lib/toastStore'`
  — in useAnalysisChat.ts: adjust relative path to `../../lib/toastStore`
  — in useWordChat.ts: adjust relative path to `../../lib/toastStore`):
  toast.error(message: string): void
  toast.success(message: string): void

From frontend/src/features/translate/api/translateApi.ts:
  export async function translate(text, sourceLang, targetLang, signal?: AbortSignal)
  — signal is already wired through to withTimeout() and all fetch calls

From backend/src/services/words.ts:
  interface UpdateWordInput extends Partial<Omit<CreateWordInput, 'tagNames'>> { personalNote?: string }
  export async function updateWord(userId, wordId, data: UpdateWordInput)
  — currently does `prisma.word.update({ where: { id: wordId }, data })` — fix: destructure only safe fields
</interfaces>

<tasks>

<task type="auto">
  <name>Task 06-01: Wire FAB + AddWordDrawer into WordsPage dictionary tab</name>
  <files>frontend/src/pages/WordsPage.tsx</files>
  <action>
    Per D-02 (CONTEXT.md): FAB and AddWordDrawer exist but are imported nowhere (B-01). Wire
    the manual add-word flow into the dictionary tab of WordsPage.

    1. Add imports at the top of WordsPage.tsx (after existing imports):
       import { FAB } from '../features/words/components/FAB';
       import { AddWordDrawer } from '../features/words/components/AddWordDrawer';

    2. Add state alongside existing useState calls:
       const [addOpen, setAddOpen] = useState(false);

    3. Render AddWordDrawer next to the existing WordDetailDrawer render site (wherever
       WordDetailDrawer is rendered, add immediately after it):
       <AddWordDrawer open={addOpen} onClose={() => setAddOpen(false)} />

    4. Render FAB inside the dictionary tab content block (the block where WordList is rendered,
       activeTab === 'dictionary' section). The existing chat FAB sits at bottom-20 right-6; place
       the add-word FAB at bottom-20 left-6 (opposite side) so they do not overlap on mobile.
       Render it ONLY when activeTab === 'dictionary':
       {activeTab === 'dictionary' && <FAB onClick={() => setAddOpen(true)} />}
       Override the FAB's fixed position by wrapping it, OR edit the FAB className to use
       bottom-20 left-6 (or pass a className prop if FAB supports it). Since FAB has hardcoded
       `bottom-6 right-6`, the safest approach without touching FAB.tsx is to render a custom
       button inline in WordsPage instead of the FAB component — use the same visual style:
       `btn btn-primary btn-circle w-14 h-14 shadow-lg fixed bottom-20 left-6 z-40 text-2xl`.
       aria-label="Додати слово вручну".

    Verify with: grep in WordsPage.tsx confirms `addOpen` state, `AddWordDrawer`, and the FAB
    button are all present.
  </action>
  <verify>
    <automated>grep -c "addOpen" "frontend/src/pages/WordsPage.tsx"</automated>
  </verify>
  <done>
    Dictionary tab shows a '+' FAB (bottom-left, does not overlap the chat FAB at bottom-right).
    Tapping it opens AddWordDrawer. Saving a word from the drawer adds it to the word list.
    Closing the drawer without saving leaves the list unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 06-02: Expand WordDetailDrawer edit mode to include forms, examples, difficulty</name>
  <files>frontend/src/features/words/components/WordDetailDrawer.tsx</files>
  <action>
    Per D-03 (CONTEXT.md): edit mode currently shows only headword, translation, wordClass, gender,
    notes (P-02 from RESEARCH.md). Backend PATCH already accepts forms, examples, difficulty.

    1. Add three state variables alongside the existing edit-mode states (around line 30-34):
       const [editDifficulty, setEditDifficulty] = useState<Difficulty | ''>('');
       const [editForms, setEditForms] = useState<WordForms>({});
       const [editExamples, setEditExamples] = useState<string[]>(['', '', '']);

       Import Difficulty and WordForms from '../api/wordsApi' (already imported Word from there —
       expand the import).

    2. In the useEffect that seeds edit state from `word` (the effect on [wordId, word]), add:
       setEditDifficulty((word.difficulty ?? '') as Difficulty | '');
       setEditForms(word.forms ?? {});
       setEditExamples(
         word.examples.length > 0
           ? [...word.examples, ...Array(Math.max(0, 3 - word.examples.length)).fill('')]
           : ['', '', '']
       );

    3. In handleEditSubmit, add these fields to the updateMutation.mutate call object:
       difficulty: editDifficulty || undefined,
       forms: Object.keys(editForms).length > 0 ? editForms : undefined,
       examples: editExamples.filter(Boolean).length > 0
         ? editExamples.filter(Boolean)
         : undefined,

    4. In the edit-mode JSX form (after the notes textarea, before the modal-action div):
       a) Difficulty SelectField:
          <SelectField id="edit-difficulty" label="Nivå (CEFR)" value={editDifficulty}
            onChange={e => setEditDifficulty(e.target.value as Difficulty | '')}>
            <option value="">— (ikkje valt)</option>
            <option value="A1">A1</option>
            <option value="A2">A2</option>
            <option value="B1">B1</option>
            <option value="B2">B2</option>
            <option value="C1">C1</option>
            <option value="C2">C2</option>
          </SelectField>

       b) Forms section — render only when wordClass is not 'sentence' and not '':
          When editWordClass is noun/verb/adjective, the user may have forms. Show a simple
          key-value editor. For each entry in editForms, show two inline inputs (key, value).
          Add a "Додати форму" button that appends an empty entry. Keep it simple:
          {editWordClass && editWordClass !== 'sentence' && (
            <div className="form-control w-full">
              <span className="label-text font-semibold mb-1 block">Форми</span>
              {Object.entries(editForms).map(([key, value]) => (
                <div key={key} className="flex gap-2 mb-2">
                  <input className="input input-bordered input-sm flex-1" value={key} readOnly />
                  <input className="input input-bordered input-sm flex-1"
                    value={value}
                    onChange={e => setEditForms(prev => ({ ...prev, [key]: e.target.value }))}
                  />
                </div>
              ))}
            </div>
          )}

       c) Examples — 3 textarea slots (same pattern as AddWordDrawer):
          <div className="form-control w-full">
            <span className="label-text font-semibold mb-1 block">Приклади</span>
            {editExamples.map((ex, i) => (
              <textarea key={i} className="textarea textarea-bordered w-full mb-2 text-sm"
                rows={2} value={ex} placeholder="Необов'язково"
                onChange={e => setEditExamples(prev => prev.map((v, j) => j === i ? e.target.value : v))}
              />
            ))}
          </div>

    Do NOT place fenced code blocks in this action — implement exactly as described above.
  </action>
  <verify>
    <automated>grep -c "editDifficulty\|editForms\|editExamples" "frontend/src/features/words/components/WordDetailDrawer.tsx"</automated>
  </verify>
  <done>
    Opening a word in edit mode shows difficulty select, forms editor, and 3 example slots
    pre-filled with existing values. Saving sends the updated forms/examples/difficulty to
    the backend PATCH endpoint, and the view mode reflects the changes immediately.
  </done>
</task>

<task type="auto">
  <name>Task 06-03: Delete confirmation + chat error toasts</name>
  <files>
    frontend/src/features/words/components/WordDetailDrawer.tsx,
    frontend/src/features/words/hooks/useAnalysisChat.ts,
    frontend/src/features/words/hooks/useWordChat.ts
  </files>
  <action>
    Three independent micro-fixes grouped because they touch overlapping files.

    === WordDetailDrawer.tsx — delete confirmation (U-07) ===

    1. Add state near the other edit states:
       const [confirmDelete, setConfirmDelete] = useState(false);

    2. Replace the current "Видалити" button in view mode (currently calls handleDelete directly)
       with a two-step flow:
       - When confirmDelete is false: show a btn-error outline button "Видалити" that sets
         confirmDelete to true (does NOT call handleDelete).
       - When confirmDelete is true: show two buttons side by side:
         * A btn-ghost "Скасувати" that sets confirmDelete to false.
         * A btn-error "Так, видалити" (with loading={deleteMutation.isPending}) that calls
           handleDelete.

       Reset confirmDelete to false whenever the drawer closes or the word changes — add
       `setConfirmDelete(false)` inside the useEffect on [open, wordId] (in the `if (!open)`
       branch AND in the `if (open)` branch so switching words also resets it).

    === useAnalysisChat.ts — toast on send failure (P-06) ===

    3. Add import at the top:
       import { toast } from '../../lib/toastStore';
       (Adjust path: useAnalysisChat.ts is at src/features/words/hooks/, toastStore is at
       src/lib/toastStore.ts — relative path is '../../../lib/toastStore'.)

    4. In the catch block (after setMessages filter line), add:
       toast.error('Не вдалося надіслати повідомлення');

    === useWordChat.ts — toast on send failure (P-06) ===

    5. Same as above: add import for toast (same relative path '../../../lib/toastStore'),
       add toast.error('Не вдалося надіслати повідомлення') in the catch block of `send`.
  </action>
  <verify>
    <automated>grep -c "confirmDelete" "frontend/src/features/words/components/WordDetailDrawer.tsx"</automated>
  </verify>
  <done>
    Tapping "Видалити" in view mode shows a confirmation row — only the second tap on
    "Так, видалити" triggers the delete mutation. Tapping "Скасувати" returns to normal view.
    When a chat message fails to send (both analysis chat and word chat), a red toast appears
    and the optimistic message is removed.
  </done>
</task>

<task type="auto">
  <name>Task 06-04: Backend security hardening (field whitelist + global error handler + auth try/catch)</name>
  <files>
    backend/src/services/words.ts,
    backend/src/index.ts,
    backend/src/routes/auth.ts
  </files>
  <action>
    Per D-04 (CONTEXT.md): no breaking changes — only additive protection.

    === backend/src/services/words.ts — PATCH field whitelist (S-05) ===

    1. Replace the body of updateWord so it explicitly picks only safe fields from `data`
       before passing to Prisma. The `data` parameter is typed as UpdateWordInput, but the
       route passes req.body directly (which could contain extra fields). Add a destructure:

       export async function updateWord(userId: string, wordId: string, data: UpdateWordInput) {
         await getWordForUser(userId, wordId);
         const {
           headword, translation, gender, wordClass, notes,
           personalNote, difficulty, forms, examples,
         } = data;
         const safeData = Object.fromEntries(
           Object.entries({
             headword, translation, gender, wordClass, notes,
             personalNote, difficulty, forms, examples,
           }).filter(([, v]) => v !== undefined)
         );
         return prisma.word.update({ where: { id: wordId }, data: safeData });
       }

       This ensures fields like userId, rawAiOutput, createdAt are stripped even if present
       in req.body.

    === backend/src/index.ts — global error handler (S-02) ===

    2. Add the following middleware at the end of index.ts, AFTER the app.get('/health', ...)
       route and BEFORE the app.listen call:

       app.use((err: unknown, _req: express.Request, res: express.Response, _next: express.NextFunction) => {
         console.error(err);
         res.status(500).json({ error: 'Internal server error' });
       });

       Import express types are already available via the existing `import express from 'express'`
       — use `express.Request`, `express.Response`, `express.NextFunction` for parameter types.

    === backend/src/routes/auth.ts — try/catch on register and login (S-01) ===

    3. Wrap the body of the register handler (router.post('/register', async (req, res) => {
       ...})) in a try/catch: wrap the existing body logic in try { ... } catch (err) { next(err); }.
       Change the handler signature to async (req, res, next) to receive next.

    4. Same wrapping for the login handler (router.post('/login', async (req, res) => {...})).

    The /logout and /me routes are short and already protected; wrapping is optional but not
    required for this task.
  </action>
  <verify>
    <automated>grep -c "safeData\|Internal server error\|next(err)" "backend/src/services/words.ts" "backend/src/index.ts" "backend/src/routes/auth.ts"</automated>
  </verify>
  <done>
    A PATCH request with body `{ userId: "evil-id", headword: "test" }` updates only headword
    and leaves userId unchanged (verified by inspecting the safeData object).
    An Express route that calls next(new Error('boom')) returns JSON { error: 'Internal server
    error' } with status 500, not a stack trace.
    Auth handlers no longer throw unhandled promise rejections on unexpected DB errors.
  </done>
</task>

<task type="auto">
  <name>Task 06-05: Translator UX polish + code cleanup + React ErrorBoundary</name>
  <files>
    frontend/src/features/translate/components/TranslatorPanel.tsx,
    frontend/src/features/translate/hooks/useTranslate.ts,
    frontend/src/features/translate/api/translateApi.ts,
    frontend/src/App.tsx
  </files>
  <action>
    Five distinct changes to TranslatorPanel.tsx, one deletion in translateApi.ts, and one
    addition in App.tsx.

    === translateApi.ts — delete dead function (B-09) ===

    1. Delete lines 6-10 (the `function requestSignal()` block). It is never called anywhere.
       The file after deletion should start: const MYMEMORY = ... then const APERTIUM = ...
       then const MYMEMORY_EMAIL = ... then const REQUEST_TIMEOUT_MS = ... then
       function withTimeout(...).

    === TranslatorPanel.tsx — five fixes ===

    2. AbortController ref + abort on unmount + pass signal to mutateAsync (B-10):
       - Add after the existing useRef line:
         const abortRef = useRef<AbortController | null>(null);
       - In the translation effect (the useEffect on [textToTranslate, direction]), before
         calling mutation.mutateAsync, create and store a new controller:
           abortRef.current?.abort();
           const controller = new AbortController();
           abortRef.current = controller;
         Then pass the signal: mutation.mutateAsync({ text: trimmed, sourceLang, targetLang,
         signal: controller.signal })
         (useTranslate hook passes signal through to translateApi.translate — verify that
         useTranslate accepts a signal option in its input type; if it does not, add
         `signal?: AbortSignal` to the input type in useTranslate and forward to translate()).
       - Add a cleanup function to the translation useEffect:
           return () => { abortRef.current?.abort(); };
         Place this as the effect's return statement.

    3. Fix lastKeyRef deduplication (B-02):
       - Line 45 currently stores: `lastKeyRef.current = { text: key, dir: direction };`
         where `key` is `"${trimmed}|${direction}"` — so `.text` ends up holding a compound
         key string, while line 37 compares `lastKeyRef.current.text === key` (which happens
         to work only because both sides have the same compound string).
       - Normalise to a single string ref instead. Change the ref type:
           const lastKeyRef = useRef<string>('');
         Then line 37: `if (lastKeyRef.current === key) return;`
         Line 45: `lastKeyRef.current = key;`
         In handleKeyDown (line 61): `lastKeyRef.current = '';`
         In handleSwap (line 73): `lastKeyRef.current = '';`
         In the clear button onClick (line 110): `lastKeyRef.current = '';`
         This is simpler and removes the accidental-correctness risk documented in B-02.

    4. Fix showLoading so loading dots disappear immediately when text is cleared (U-06):
       - Replace `const isTranslating = mutation.isPending;` with:
           const showLoading = mutation.isPending && sourceText.trim().length > 0;
       - In the JSX, replace all uses of `isTranslating` with `showLoading`.
         (Used in: the loading dots span next to targetLabel, and the swap button disabled prop.)

    5. Char counter below source textarea (U-05):
       - After the closing `</div>` of the source textarea's `<div className="relative">` wrapper,
         add (inside the `<div className="form-control w-full flex-1">` container):
           <div className={`text-xs mt-1 text-right ${sourceText.length > 400 ? 'text-error' : 'text-base-content/40'}`}>
             {sourceText.length} / ~500
           </div>
         Show the counter always (not just when text is present), so the user knows the limit
         exists before hitting it. Turns red (text-error) when sourceText.length > 400.

    6. Improve fallback warning text (U-04):
       - Replace the existing fallback warning text "Результат може містити Bokmål форми" with:
         "Apertium недоступний — переклад може бути на Bokmål"

    === App.tsx — React ErrorBoundary (U-10) ===

    7. Add a class-based ErrorBoundary before the App function. React class components require
       `import React from 'react'` — add it if not already present (check existing imports first;
       if using `import { ... } from 'react'` only, add `import React from 'react'` on the same
       line or as a separate import).

       Add the ErrorBoundary class:
         class ErrorBoundary extends React.Component<
           { children: React.ReactNode },
           { hasError: boolean }
         > {
           state = { hasError: false };
           static getDerivedStateFromError() { return { hasError: true }; }
           render() {
             if (this.state.hasError) {
               return (
                 <div className="flex items-center justify-center min-h-screen p-8">
                   <div className="text-center">
                     <p className="text-error text-lg font-semibold mb-2">Щось пішло не так.</p>
                     <p className="text-base-content/60 text-sm">Оновіть сторінку, щоб продовжити.</p>
                   </div>
                 </div>
               );
             }
             return this.props.children;
           }
         }

       Wrap the <Routes> element in the App function with <ErrorBoundary>:
         <ErrorBoundary>
           <Routes>
             ...
           </Routes>
         </ErrorBoundary>

    Keep Toaster outside ErrorBoundary so toast notifications still render even if Routes crashes.
  </action>
  <verify>
    <automated>grep -c "requestSignal" "frontend/src/features/translate/api/translateApi.ts"</automated>
  </verify>
  <done>
    translateApi.ts no longer contains `requestSignal`. TranslatorPanel shows a char counter
    (turns red above 400 chars). The fallback warning reads "Apertium недоступний — переклад
    може бути на Bokmål". Loading dots disappear immediately when the textarea is cleared.
    Switching tabs while translation is in flight aborts the request (network tab shows abort).
    App.tsx wraps Routes with ErrorBoundary — deliberately throwing in a child component shows
    the Ukrainian error message instead of a blank screen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/words PATCH | Authenticated user sends arbitrary JSON body; currently no field filtering |
| Express unhandled errors → client | Internal error details (Prisma schema, stack trace) exposed if no error handler |
| Auth routes → Prisma | Unhandled DB exceptions propagate as raw 500 responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | services/words.ts updateWord | mitigate | Destructure only whitelisted fields (headword, translation, gender, wordClass, notes, personalNote, difficulty, forms, examples) before passing to prisma.update — per Task 06-04 |
| T-06-02 | Information Disclosure | backend/src/index.ts | mitigate | Global error handler returns `{ error: 'Internal server error' }` — no stack trace or Prisma schema details leaked — per Task 06-04 |
| T-06-03 | Denial of Service | routes/auth.ts | mitigate | try/catch wrapping prevents unhandled rejections from crashing the process — per Task 06-04 |
| T-06-04 | Spoofing | JWT localStorage persistence | accept | Personal single-user app; documented known limitation per D-04 in CONTEXT.md (S-03 out of scope) |
| T-06-05 | Information Disclosure | translateApi MYMEMORY_EMAIL URL param | accept | Personal app; email in URL is low-risk per CONTEXT.md out-of-scope decision (S-04) |
</threat_model>

<verification>
After all 5 tasks execute:

1. Manual add flow: navigate to dictionary tab — a '+' FAB appears at bottom-left. Tap it — AddWordDrawer opens. Enter a word, save — it appears in the word list.

2. Edit completeness: open any word with AI-filled forms/examples/difficulty. Tap edit — see difficulty select, forms grid, 3 example textareas pre-filled. Change a value, save — view mode shows the updated value.

3. Delete safety: open any word, tap "Видалити" — confirm buttons appear. Tap "Скасувати" — back to normal. Tap "Видалити" again then "Так, видалити" — word deleted and drawer closes.

4. Chat error toast: simulate a network failure in DevTools while in the word chat or analysis chat — the optimistic message is removed AND a red toast "Не вдалося надіслати повідомлення" appears.

5. Backend field whitelist: make a PATCH request to /api/words/:id with body `{"userId":"other","headword":"test"}` using a valid JWT. The word's userId is unchanged; only headword is updated.

6. Global error handler: call any route that triggers next(new Error('test')) — response is `{"error":"Internal server error"}` with status 500, no stack trace.

7. Translator char counter: type 401+ characters in the translator — counter turns red. Clear text — counter returns to 0 / grey.

8. Translator fallback text: break Apertium (devtools block apertium.org) — fallback warning reads "Apertium недоступний — переклад може бути на Bokmål".

9. ErrorBoundary: temporarily throw in a child component — page shows Ukrainian error message, not blank screen.

10. Dead code removed: grep for `requestSignal` in translateApi.ts returns 0 matches.
</verification>

<success_criteria>
- FAB відображається на вкладці "Словник" (bottom-left) і відкриває AddWordDrawer
- WordDetailDrawer в edit mode показує і зберігає difficulty, forms, examples (10 полів всього)
- Видалення вимагає двох натискань; "Скасувати" повертає без видалення
- toast.error з'являється при помилці надсилання в обох chat-хуках
- Backend PATCH: поля userId/rawAiOutput/createdAt ігноруються навіть якщо присутні в тілі
- Express повертає `{"error":"Internal server error"}` замість stack trace
- Auth handlers не кидають unhandled rejections
- Translator: лічильник символів (червоний > 400), покращений fallback warning
- translateApi.ts: `requestSignal` видалено
- App.tsx: ErrorBoundary обгортає Routes, показує Ukrainian fallback при render crash
</success_criteria>

<output>
Після виконання всіх 5 завдань створи:
`.planning/phases/06-polish/06-01-SUMMARY.md`

SUMMARY має містити:
- Які файли змінено і що конкретно зроблено в кожному
- Які audit findings (B-01, P-02, U-07, тощо) закрито
- Будь-які відхилення від плану або рішення прийняті під час виконання
- Commit hash(es) для кожного завдання
</output>
