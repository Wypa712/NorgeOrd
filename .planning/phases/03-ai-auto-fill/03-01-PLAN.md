---
phase: 03-ai-auto-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/ai.ts
  - backend/src/routes/words.ts
  - backend/src/services/words.ts
  - backend/.env.example
autonomous: true
requirements:
  - WORD-01

must_haves:
  truths:
    - "POST /api/words/analyze з валідним headword повертає streaming SSE-відповідь у форматі Vercel AI SDK"
    - "POST /api/words/analyze без headword повертає 400"
    - "POST /api/words/analyze без токена повертає 401"
    - "AI-відповідь містить translation, gender, wordClass, difficulty, forms, examples, tags"
    - "forms для іменника hus містить pl_def: 'husa' (Nynorsk), НЕ 'husene' (Bokmål)"
    - "POST /api/words з tagNames масивом зберігає теги через connectOrCreate"
    - "GET /api/words повертає слова з включеними tags (wordId, tag.id, tag.name)"
  artifacts:
    - path: "backend/src/services/ai.ts"
      provides: "Zod schema + Nynorsk prompt builder + analyzeWord function"
      exports: ["wordAnalysisSchema", "analyzeWord"]
    - path: "backend/src/routes/words.ts"
      provides: "POST /api/words/analyze endpoint"
      contains: "router.post('/analyze', ...)"
    - path: "backend/src/services/words.ts"
      provides: "createWord з тегами, listWords/getWordForUser з includes"
      exports: ["listWords", "createWord", "getWordForUser", "updateWord", "deleteWord"]
  key_links:
    - from: "backend/src/routes/words.ts"
      to: "backend/src/services/ai.ts"
      via: "analyzeWord(headword)"
      pattern: "analyzeWord"
    - from: "backend/src/services/ai.ts"
      to: "ai + @ai-sdk/groq"
      via: "streamObject + createGroq"
      pattern: "streamObject"
    - from: "backend/src/services/words.ts"
      to: "backend/src/lib/prisma.ts"
      via: "prisma.word.create with tags.create"
      pattern: "connectOrCreate"
---

## Phase Goal

**As a** Norwegian learner, **I want to** type one word and have AI fill in all Nynorsk forms, **so that** building my vocabulary is effortless.

<objective>
Реалізувати backend AI pipeline: Groq `llama-3.3-70b-versatile` через Vercel AI SDK `streamObject` → стрімінг JSON-відповіді → Zod-валідація. Оновити words service для підтримки тегів і нових полів при збереженні.

Purpose: Frontend-плани (03-02, 03-03) залежать від живого `/api/words/analyze` endpoint і оновленого `createWord` з тегами.

Output: `backend/src/services/ai.ts`, оновлений `backend/src/routes/words.ts`, оновлений `backend/src/services/words.ts`.
</objective>

<execution_context>
@C:\Users\Олександр\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Олександр\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@c:\Users\Олександр\Desktop\lern nor\.planning\PROJECT.md
@c:\Users\Олександр\Desktop\lern nor\.planning\STATE.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\03-ai-auto-fill\03-CONTEXT.md

<interfaces>
<!-- Ключові контракти -->

From backend/prisma/schema.prisma:
  Word.forms = Json? (зберігаємо як Record<string, string>)
  Word.examples = String[] (масив рядків)
  Word.tags = WordTag[] (junction: wordId + tagId)
  Tag.name = String @unique
  WordTag: { wordId, tagId, word: Word, tag: Tag } @@id([wordId, tagId])

From backend/src/middleware/auth.ts:
  requireAuth інжектує req.user.userId — вже застосований на рівні index.ts для /api/words

Vercel AI SDK (пакет `ai`, версія 4.x):
  import { streamObject } from 'ai';
  import { createGroq } from '@ai-sdk/groq';
  Зод: import { z } from 'zod';
  Використання: streamObject({ model, schema, prompt }) → result
  result.pipeDataStreamToResponse(res) — пайпає SSE stream до Express response

AI SDK data stream format (що записує pipeDataStreamToResponse):
  Content-Type: text/plain; charset=utf-8
  Transfer-Encoding: chunked
  Рядки типу: 2:[{...partial object...}]\n
  Frontend useObject читає цей формат автоматично.

GROQ_API_KEY:
  createGroq() без аргументів читає process.env.GROQ_API_KEY автоматично.
  Якщо ключ відсутній — кидає помилку при першому виклику.

Canary (КРИТИЧНО):
  Якщо модель повертає huset / ikke / jeg замість husa / ikkje / eg — Bokmål, помилка.
  Промпт ПОВИНЕН явно вказувати Nynorsk і містити ці приклади.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Встановити пакети + додати GROQ_API_KEY в .env.example</name>
  <files>backend/package.json, backend/.env.example</files>
  <action>
    В директорії `c:\Users\Олександр\Desktop\lern nor\backend` виконай:
    ```
    npm install ai @ai-sdk/groq zod
    ```

    Додай в кінець backend/.env.example:
    ```
    GROQ_API_KEY=gsk_replace_with_your_groq_api_key
    ```
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\backend" && node -e "require('ai'); require('@ai-sdk/groq'); require('zod'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/ai` існує
    - `node_modules/@ai-sdk/groq` існує
    - `node_modules/zod` існує
    - backend/.env.example містить рядок GROQ_API_KEY=
  </acceptance_criteria>
  <done>npm install успішний, всі 3 пакети доступні.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Створити backend/src/services/ai.ts</name>
  <files>backend/src/services/ai.ts</files>
  <read_first>
    - backend/prisma/schema.prisma — Gender, WordClass, Difficulty enums
  </read_first>
  <behavior>
    - `wordAnalysisSchema` — Zod object schema для AI-відповіді
    - `buildAnalysisPrompt(headword)` — Nynorsk-специфічний промпт з canary прикладами
    - `analyzeWord(headword)` — викликає `streamObject` і повертає `StreamObjectResult`
  </behavior>
  <action>
    Створи файл `backend/src/services/ai.ts`.

    Імпорти:
    ```ts
    import { streamObject } from 'ai';
    import { createGroq } from '@ai-sdk/groq';
    import { z } from 'zod';
    ```

    Оголоси та експортуй `wordAnalysisSchema`:
    ```ts
    export const wordAnalysisSchema = z.object({
      translation: z.string().optional(),
      gender: z.enum(['masculine', 'feminine', 'neuter']).optional(),
      wordClass: z.enum(['noun', 'verb', 'adjective', 'adverb', 'other']).optional(),
      difficulty: z.enum(['A1', 'A2', 'B1', 'B2', 'C1', 'C2']).optional(),
      forms: z.record(z.string()).optional(),
      examples: z.array(z.string()).optional(),
      tags: z.array(z.string()).optional(),
    });

    export type WordAnalysis = z.infer<typeof wordAnalysisSchema>;
    ```

    Приватна функція `buildAnalysisPrompt(headword: string): string`:
    ```
    You are an expert in Nynorsk Norwegian (nynorsk), one of the two written standards of Norwegian.
    Analyze the Nynorsk word "${headword}" and return structured data.

    CRITICAL — Use ONLY Nynorsk forms, never Bokmål:
    - Plural definite of "hus" → "husa" (NOT "husene" which is Bokmål)
    - Negation → "ikkje" (NOT "ikke")
    - First person singular pronoun → "eg" (NOT "jeg")
    - Feminine nouns use -a suffix in definite: "boka", "sola", "jenta"
    - Verb infinitives often end in -a in Nynorsk: "skriva", "lesa", "gjera"

    Return a JSON object with these fields:
    - translation: Ukrainian translation
    - gender: "masculine", "feminine", or "neuter" (for nouns only, omit for other classes)
    - wordClass: one of "noun", "verb", "adjective", "adverb", "other"
    - difficulty: CEFR level — one of "A1", "A2", "B1", "B2", "C1", "C2"
    - forms: flat object with Nynorsk inflection forms:
        nouns: { sing_indef, sing_def, pl_indef, pl_def }
        verbs: { inf, pres, past, past_part }
        adjectives: { positive, comparative, superlative }
        adverbs/other: {} (empty object)
    - examples: array of 2-3 short Nynorsk example sentences
    - tags: array of 2-4 English topic tags (e.g. "house", "nature", "body", "daily life")
    ```

    Ініціалізуй `groq` через `createGroq()` (читає `GROQ_API_KEY` з process.env автоматично):
    ```ts
    const groq = createGroq();
    ```

    Оголоси та експортуй функцію `analyzeWord`:
    ```ts
    export function analyzeWord(headword: string) {
      return streamObject({
        model: groq('llama-3.3-70b-versatile'),
        schema: wordAnalysisSchema,
        prompt: buildAnalysisPrompt(headword),
      });
    }
    ```

    Функція `analyzeWord` НЕ async — `streamObject` повертає `StreamObjectResult` синхронно.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\backend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Файл backend/src/services/ai.ts існує
    - TypeScript компілюється без помилок
    - Промпт містить слово "Nynorsk" та приклади "husa", "ikkje", "eg"
    - `wordAnalysisSchema` експортований з полями: translation, gender, wordClass, difficulty, forms, examples, tags
    - `analyzeWord` повертає результат `streamObject` (не Promise)
  </acceptance_criteria>
  <done>ai.ts компілюється, schema і analyzeWord експортовані.</done>
</task>

<task type="auto">
  <name>Task 3: Додати POST /api/words/analyze в words router</name>
  <files>backend/src/routes/words.ts</files>
  <read_first>
    - backend/src/routes/words.ts — існуючі routes, pattern для guards
    - backend/src/services/ai.ts — щойно створений (Task 2)
  </read_first>
  <action>
    Відкрий `backend/src/routes/words.ts`.

    Додай імпорт на початку файлу (після існуючих імпортів):
    ```ts
    import { analyzeWord } from '../services/ai';
    ```

    Додай новий route ПЕРЕД рядком `router.get('/', ...)` (перший route):
    ```ts
    router.post('/analyze', async (req, res, next) => {
      const headword = req.body?.headword;
      if (!headword || typeof headword !== 'string' || !headword.trim()) {
        return res.status(400).json({ error: 'headword is required' });
      }
      try {
        const result = analyzeWord(headword.trim());
        result.pipeDataStreamToResponse(res);
      } catch (err) {
        next(err);
      }
    });
    ```

    ВАЖЛИВО: Route `/analyze` ПОВИНЕН бути перед `/:id` — інакше Express матче "analyze" як :id параметр.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\backend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/words.ts містить `router.post('/analyze', ...)`
    - Route /analyze розташований ПЕРЕД `router.get('/:id', ...)`
    - Без headword → 400
    - TypeScript компілюється без помилок
    - Існуючі 5 routes (GET /, POST /, GET /:id, PATCH /:id, DELETE /:id) не змінені
  </acceptance_criteria>
  <done>POST /api/words/analyze зареєстрований, TypeScript чистий.</done>
</task>

<task type="auto">
  <name>Task 4: Оновити words service — теги + нові поля</name>
  <files>backend/src/services/words.ts</files>
  <read_first>
    - backend/src/services/words.ts — поточна версія
    - backend/prisma/schema.prisma — WordTag, Tag models, Word.forms, Word.examples
  </read_first>
  <behavior>
    - `CreateWordInput` розширюється: додаються forms, examples, tagNames, difficulty
    - `createWord` обробляє `tagNames` через Prisma `connectOrCreate` на Tag
    - `listWords` повертає слова з включеними тегами: `include: { tags: { include: { tag: true } } }`
    - `getWordForUser` аналогічно включає теги
    - `updateWord` — без змін (теги не редагуються в MVP edit)
  </behavior>
  <action>
    Відкрий `backend/src/services/words.ts` і внеси такі зміни:

    **1. Оновити `CreateWordInput` interface** — додати нові поля:
    ```ts
    interface CreateWordInput {
      headword: string;
      translation?: string;
      gender?: 'masculine' | 'feminine' | 'neuter';
      wordClass?: 'noun' | 'verb' | 'adjective' | 'adverb' | 'other';
      notes?: string;
      forms?: Record<string, string>;
      examples?: string[];
      tagNames?: string[];
      difficulty?: 'A1' | 'A2' | 'B1' | 'B2' | 'C1' | 'C2';
    }
    ```

    **2. Оновити `createWord`** — виділити `tagNames` із `data`, додати `connectOrCreate` для тегів, включити tags в include:
    ```ts
    export async function createWord(userId: string, data: CreateWordInput) {
      const { tagNames, ...wordData } = data;
      try {
        return await prisma.word.create({
          data: {
            userId,
            ...wordData,
            ...(tagNames?.length ? {
              tags: {
                create: tagNames.map(name => ({
                  tag: {
                    connectOrCreate: {
                      where: { name },
                      create: { name },
                    },
                  },
                })),
              },
            } : {}),
          },
          include: { tags: { include: { tag: true } } },
        });
      } catch (err) {
        if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002') {
          throw Object.assign(new Error('Duplicate headword'), { statusCode: 409 });
        }
        throw err;
      }
    }
    ```

    **3. Оновити `listWords`** — додати include:
    ```ts
    export async function listWords(userId: string) {
      return prisma.word.findMany({
        where: { userId },
        include: { tags: { include: { tag: true } } },
        orderBy: { createdAt: 'desc' },
      });
    }
    ```

    **4. Оновити `getWordForUser`** — додати include:
    ```ts
    export async function getWordForUser(userId: string, wordId: string) {
      const word = await prisma.word.findUnique({
        where: { id: wordId },
        include: { tags: { include: { tag: true } } },
      });
      if (!word) throw Object.assign(new Error('Not found'), { statusCode: 404 });
      if (word.userId !== userId) throw Object.assign(new Error('Forbidden'), { statusCode: 403 });
      return word;
    }
    ```

    `updateWord` і `deleteWord` — без змін.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\backend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `CreateWordInput` містить поля: forms, examples, tagNames, difficulty
    - `createWord` використовує `connectOrCreate` для тегів (коли tagNames переданий)
    - `listWords` має `include: { tags: { include: { tag: true } } }`
    - `getWordForUser` має аналогічний include
    - TypeScript компілюється без помилок
    - `updateWord` і `deleteWord` — без змін
  </acceptance_criteria>
  <done>Words service оновлений, TypeScript чистий. GET /api/words тепер повертає теги для кожного слова.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /api/words/analyze body | Untrusted: headword — validate non-empty string before sending to Groq |
| Groq API response | External: AI може повернути Bokmål — Zod schema не перевіряє мову, тільки структуру |
| tagNames array | Untrusted: довільні рядки — connectOrCreate безпечний, але теги зберігаються as-is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01 | Spoofing | POST /analyze | mitigate | requireAuth застосований в index.ts — 401 без токена |
| T-03-02 | Tampering | headword → Groq prompt | mitigate | headword вставляється в промпт як текст; Groq не виконує код |
| T-03-03 | Denial of Service | Groq API calls | accept | Groq free tier: 30 req/min — для особистого використання достатньо |
| T-03-04 | Information Disclosure | Groq request | accept | headword надсилається до Groq — не чутливі дані |
| T-03-05 | Tampering | tagNames input | mitigate | connectOrCreate — дублікати обробляються безпечно; userId з JWT, не з body |
</threat_model>

<verification>
Після виконання цього плану перевірити вручну (потрібен запущений backend + GROQ_API_KEY в .env):

```bash
# Отримати TOKEN (як в Phase 2)
# Потім перевірити analyze endpoint:

curl -X POST http://localhost:3000/api/words/analyze \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"headword":"hus"}' \
  --no-buffer

# Очікується: streaming JSON chunks
# Критично: pl_def повинно бути "husa" (Nynorsk), НЕ "husene" (Bokmål)

# Без токена:
curl -X POST http://localhost:3000/api/words/analyze \
  -d '{"headword":"hus"}' -o /dev/null -w "%{http_code}"
# → 401

# Без headword:
curl -X POST http://localhost:3000/api/words/analyze \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{}' -o /dev/null -w "%{http_code}"
# → 400

# Зберегти слово з тегами:
curl -X POST http://localhost:3000/api/words \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"headword":"sol","tagNames":["nature","sky"]}'
# → 201 з tags масивом в відповіді
```
</verification>

<success_criteria>
- `POST /api/words/analyze` стрімить Vercel AI SDK відповідь (forms, translation, тощо)
- AI повертає Nynorsk форми: `pl_def` іменника `hus` = `husa`
- `POST /api/words/analyze` без headword → 400; без токена → 401
- `POST /api/words` з `tagNames` зберігає теги в DB через `connectOrCreate`
- `GET /api/words` включає `tags: [{ tagId, tag: { id, name } }]` для кожного слова
- `npx tsc --noEmit` проходить без помилок
</success_criteria>

<output>
Після завершення створи `.planning/phases/03-ai-auto-fill/03-01-SUMMARY.md`
</output>
