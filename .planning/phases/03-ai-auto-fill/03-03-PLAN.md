---
phase: 03-ai-auto-fill
plan: 03
type: execute
gap_closure: true
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/src/features/words/api/wordsApi.ts
  - frontend/src/features/words/components/WordDetailDrawer.tsx
  - frontend/src/features/words/components/WordListRow.tsx
  - frontend/src/features/words/components/WordList.tsx
  - CLAUDE.md
autonomous: true
requirements:
  - WORD-01

must_haves:
  truths:
    - "WordDetailDrawer displays saved forms in a compact key/value grid"
    - "D-01: Phase 3 active AI provider remains Groq @ai-sdk/groq with GROQ_API_KEY and llama-3.3-70b-versatile"
    - "D-02: CLAUDE.md is updated so OpenAI gpt-4o-mini is not documented as the active Phase 3 auto-fill provider"
    - "D-11: WordDetailDrawer displays forms in a compact mobile-friendly key/value grid"
    - "D-17: Live canary verification covers husa, ikkje, and eg and flags Bokmal outputs as critical"
    - "WordDetailDrawer displays saved tags as DaisyUI badges"
    - "WordDetailDrawer displays saved examples"
    - "WordDetailDrawer displays saved difficulty"
    - "WordListRow or WordList surfaces at least tags or difficulty without breaking existing list layout"
    - "CLAUDE.md no longer claims Phase 3 uses OpenAI gpt-4o-mini as the active auto-fill provider"
    - "Live canary verification instructions cover husa, ikkje, and eg"
  artifacts:
    - path: "frontend/src/features/words/components/WordDetailDrawer.tsx"
      provides: "Persisted AI forms/tags/examples/difficulty display"
    - path: "frontend/src/features/words/components/WordListRow.tsx"
      provides: "Compact AI metadata affordance in list rows"
    - path: "CLAUDE.md"
      provides: "Updated AI provider documentation"
  key_links:
    - from: "frontend/src/features/words/components/WordDetailDrawer.tsx"
      to: "frontend/src/features/words/api/wordsApi.ts"
      via: "Word.forms, Word.tags, Word.examples, Word.difficulty"
      pattern: "Word"
    - from: "frontend/src/features/words/components/WordListRow.tsx"
      to: "frontend/src/features/words/api/wordsApi.ts"
      via: "Word.tags and Word.difficulty"
      pattern: "Word"
---

## Phase Goal

**As a** Norwegian learner, **I want to** see the AI-generated forms, examples, tags, and difficulty after saving, **so that** the auto-filled word card is useful beyond the add flow.

<objective>
Close the remaining Phase 3 display/documentation/verification gaps: show persisted AI metadata in the word detail UI, keep the list scannable with AI metadata, update stale provider docs, and perform live canary verification if `GROQ_API_KEY` is available.

Output: WordDetailDrawer/WordList display support for forms/tags/examples/difficulty, updated CLAUDE.md provider note, and a Phase 3 verification rerun path that can pass once live Groq canaries are checked.
</objective>

<execution_context>
@C:\Users\Олександр\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\Олександр\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@c:\Users\Олександр\Desktop\lern nor\.planning\PROJECT.md
@c:\Users\Олександр\Desktop\lern nor\.planning\STATE.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\03-ai-auto-fill\03-CONTEXT.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\03-ai-auto-fill\03-VERIFICATION.md
@c:\Users\Олександр\Desktop\lern nor\.planning\phases\03-ai-auto-fill\03-01-SUMMARY.md

<interfaces>
Word fields after 03-01 and 03-02:
  forms: Record<string, string> | null
  examples: string[]
  tags: { wordId: string; tagId: string; tag: { id: string; name: string } }[]
  difficulty: 'A1' | 'A2' | 'B1' | 'B2' | 'C1' | 'C2' | null

Display requirements from 03-CONTEXT:
  - Forms table: compact 2x2-ish key/value grid, mobile-friendly
  - Tags: DaisyUI badges/chips
  - Difficulty: editable in add flow, visible after save
  - Examples: Nynorsk example sentences visible in detail card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure Word type carries persisted AI metadata</name>
  <files>frontend/src/features/words/api/wordsApi.ts</files>
  <read_first>
    - frontend/src/features/words/api/wordsApi.ts
    - backend/src/services/words.ts
    - backend/prisma/schema.prisma
  </read_first>
  <action>
    Verify and, if needed, update `Word` in `wordsApi.ts` so it includes:
    ```ts
    forms: WordForms | null;
    examples: string[];
    tags: WordTag[];
    difficulty: Difficulty | null;
    ```

    If Plan 03-02 already added the types exactly, do not rewrite unrelated code.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\frontend" && npm.cmd run build</automated>
  </verify>
  <acceptance_criteria>
    - `Word` has `forms: WordForms | null`
    - `Word` has `examples: string[]`
    - `Word` has `tags: WordTag[]`
    - `Word` has `difficulty: Difficulty | null`
    - frontend build passes
  </acceptance_criteria>
  <done>Persisted AI metadata is typed for UI display.</done>
</task>

<task type="auto">
  <name>Task 2: Display forms, examples, tags, and difficulty in WordDetailDrawer</name>
  <files>frontend/src/features/words/components/WordDetailDrawer.tsx</files>
  <read_first>
    - frontend/src/features/words/components/WordDetailDrawer.tsx
    - frontend/src/features/words/components/GenderBadge.tsx
    - frontend/src/features/words/components/WordClassBadge.tsx
    - frontend/src/features/words/api/wordsApi.ts
  </read_first>
  <action>
    Update view mode in `WordDetailDrawer.tsx`.

    Add helper render sections inside the component:
    - `formEntries = word.forms ? Object.entries(word.forms).filter(([, value]) => value) : []`
    - `tagNames = word.tags?.map(({ tag }) => tag.name) ?? []`

    In `mode === 'view'`:
    1. Show difficulty near existing badges if present:
       ```tsx
       {word.difficulty && <span className="badge badge-neutral">{word.difficulty}</span>}
       ```
    2. Show examples when `word.examples.length > 0`:
       - Section label: `Приклади`
       - Use `ul`/`li` or compact stacked paragraphs.
    3. Show forms when entries exist:
       - Section label: `Форми`
       - Use `grid grid-cols-2 gap-2 text-sm` or equivalent.
       - Render key label and value; do not hide keys.
    4. Show tags when `tagNames.length > 0`:
       - Section label: `Теги`
       - Render each as `badge badge-outline`.

    Keep edit/delete actions visible and do not change edit form behavior in this task.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\frontend" && npm.cmd run build</automated>
  </verify>
  <acceptance_criteria>
    - WordDetailDrawer contains section text `Приклади`
    - WordDetailDrawer contains section text `Форми`
    - WordDetailDrawer contains section text `Теги`
    - WordDetailDrawer renders `word.difficulty`
    - WordDetailDrawer reads `word.forms`
    - WordDetailDrawer maps `word.tags`
    - frontend build passes
  </acceptance_criteria>
  <done>Saved AI metadata is visible in the detail drawer.</done>
</task>

<task type="auto">
  <name>Task 3: Surface compact AI metadata in the word list</name>
  <files>frontend/src/features/words/components/WordListRow.tsx, frontend/src/features/words/components/WordList.tsx</files>
  <read_first>
    - frontend/src/features/words/components/WordListRow.tsx
    - frontend/src/features/words/components/WordList.tsx
    - frontend/src/features/words/api/wordsApi.ts
  </read_first>
  <action>
    Inspect current `WordListRow.tsx`.

    Add compact metadata without changing the row click behavior:
    - If `word.difficulty` exists, show a small neutral badge with the difficulty.
    - If `word.tags` has entries, show up to 2 tag badges with tag names.
    - If there are more than 2 tags, show `+N`.

    Keep layout mobile-safe:
    - Badge container must wrap.
    - Do not make the row taller than necessary.
    - Preserve existing headword/translation display.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\frontend" && npm.cmd run build</automated>
  </verify>
  <acceptance_criteria>
    - WordListRow references `word.difficulty`
    - WordListRow references `word.tags`
    - WordListRow limits visible tag badges to 2 or fewer before `+N`
    - Existing row click/select behavior remains intact
    - frontend build passes
  </acceptance_criteria>
  <done>Word list remains scannable while surfacing AI metadata.</done>
</task>

<task type="auto">
  <name>Task 4: Update provider docs and run live canary verification path</name>
  <files>CLAUDE.md, .planning/phases/03-ai-auto-fill/03-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/03-ai-auto-fill/03-CONTEXT.md
    - .planning/phases/03-ai-auto-fill/03-VERIFICATION.md
    - backend/.env
    - backend/src/services/ai.ts
  </read_first>
  <action>
    Update `CLAUDE.md` only where it describes the active AI provider/model for auto-fill:
    - Replace stale OpenAI `gpt-4o-mini` Phase 3 guidance with Groq `llama-3.3-70b-versatile`.
    - Keep historical notes if they are clearly labeled as superseded.

    Run verification commands:
    ```powershell
    cd "c:\Users\Олександр\Desktop\lern nor\backend"
    npm.cmd run type-check
    ```

    If `backend/.env` contains a non-placeholder `GROQ_API_KEY`, run the live canary check manually with the running backend:
    - Authenticate or reuse a valid token.
    - Call `POST http://localhost:3000/api/words/analyze` for `hus`, `ikkje`, and `eg`.
    - Confirm `hus` response includes Nynorsk `husa` and not Bokmal `husene`.
    - Confirm `ikkje` is not changed to `ikke`.
    - Confirm `eg` is not changed to `jeg`.

    If a valid key/token is not available, update `03-VERIFICATION.md` with `human_needed` for the live canary only, and include exact manual commands.
  </action>
  <verify>
    <automated>cd "c:\Users\Олександр\Desktop\lern nor\backend" && npm.cmd run type-check</automated>
    <manual>Live Groq canary check for hus/ikkje/eg if GROQ_API_KEY and JWT token are available.</manual>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` mentions Groq `llama-3.3-70b-versatile` for Phase 3 auto-fill
    - `CLAUDE.md` does not present OpenAI `gpt-4o-mini` as the active Phase 3 auto-fill provider
    - backend type-check passes
    - `03-VERIFICATION.md` records either live canary PASS results or exact remaining human_needed canary commands
  </acceptance_criteria>
  <done>Provider docs are current and live canary verification is either passed or explicitly queued as human-needed.</done>
</task>

</tasks>

<verification>
Run:

```powershell
cd "c:\Users\Олександр\Desktop\lern nor\frontend"
npm.cmd run build

cd "c:\Users\Олександр\Desktop\lern nor\backend"
npm.cmd run type-check
```

Manual browser check:
1. Save an AI-filled word from AddWordDrawer.
2. Open the saved word detail drawer.
3. Confirm forms, examples, tags, and difficulty are visible.
4. Confirm list row still opens the detail drawer and shows compact metadata.

Live canary check if credentials are available:
1. Analyze `hus` and confirm `husa`, not `husene`.
2. Analyze `ikkje` and confirm not `ikke`.
3. Analyze `eg` and confirm not `jeg`.
</verification>

<success_criteria>
- Saved AI metadata is visible after persistence.
- List rows remain usable and display compact metadata.
- Provider docs match the implemented Groq backend.
- Build/type-check pass.
- Live Groq canary is passed or explicitly captured as human-needed with commands.
</success_criteria>

<output>
Після завершення створи `.planning/phases/03-ai-auto-fill/03-03-SUMMARY.md`.
</output>
